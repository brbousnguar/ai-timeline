---
description: Update timeline event links
---

To automatically match and update the event links in `script.js` using the data defined in `add_links.py`:

1. Run the Python update script
// turbo
```bash
python3 add_links.py
```
